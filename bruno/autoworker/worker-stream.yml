info:
  name: worker-stream
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/api/workers/{{WORKER_ID}}/stream"
  auth:
    type: bearer
    token: "{{API_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: false
  maxRedirects: 0
