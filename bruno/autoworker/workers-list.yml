info:
  name: workers-list
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/api/workers"
  auth:
    type: bearer
    token: "{{API_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 10000
  followRedirects: true
  maxRedirects: 5
